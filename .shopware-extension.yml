build:
    zip:
        pack:
            excludes:
                paths:
                    - .gitignore
                    - release
                    - phpcs.xml
                    - phpunit.xml.dist
                    - "*.zip"
                    - "*.phar"
